services:
  research:
    build: .
    container_name: research-python

    working_dir: /workspace

    volumes:
      - .:/workspace

    ports:
      - "8888:8888"

    stdin_open: true
    tty: true
